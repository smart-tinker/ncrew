# Workflow доставки новой функциональности (Feature)

## Цель
Реализация новой функциональности через TDD.

## Процесс

### 1. REQs & SPECs
```bash
# Сформулировать требования (User Story).
# Создать техническую спецификацию в .memory_bank/specs/.
```

### 2. Plan
```bash
# Декомпозировать задачу.
```

### 3. Branching
```bash
git checkout -b feature/name
```

### 4. Test Fail (RED)
```bash
# Написать тест, который проверяет новую функциональность.
# Запустить тест. Он ДОЛЖЕН упасть (или не скомпилироваться).
# Это подтверждает, что фичи еще нет и тест написан верно.
```

### 5. Code (GREEN)
```bash
# Написать минимальный код, чтобы тест прошел.
# Не писать лишнего функционала.
```

### 6. Test Pass & Refactor (REFACTOR)
```bash
# Запустить тесты -> PASS.
# При необходимости отрефакторить код (улучшить читаемость), сохраняя тесты зелеными.
```

### 7. Review & Integration
```bash
# PR -> Merge.
```