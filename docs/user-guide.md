# Руководство пользователя NCrew

Это руководство поможет вам начать работу с NCrew - системой для управления AI-агентами в изолированных Git Worktree.

## Содержание

1. [Добавление проекта](#добавление-проекта)
2. [Создание задачи](#создание-задачи)
3. [Запуск агента](#запуск-агента)
4. [Мониторинг выполнения](#мониторинг-выполнения)
5. [Проверка результатов](#проверка-результатов)

## Добавление проекта

### Шаг 1: Укажите путь к проекту

1. Откройте веб-интерфейс NCrew по адресу http://localhost:3000
2. Нажмите кнопку "Add Project"
3. В поле "Project Path" укажите полный путь к вашему проекту на диске
4. (Опционально) Выберите модель и провайдера для AI-агента
5. Нажмите "Save"

### Шаг 2: Настройте папку задач

Убедитесь, что в вашем проекте существует папка `.memory_bank/tasks/`:

```bash
mkdir -p /путь/к/вашему/проекту/.memory_bank/tasks
```

## Создание задачи

### Формат задачи

Задачи создаются в виде MD-файлов с frontmatter:

```markdown
---
title: Add authentication
status: New
priority: High
---

# Добавить систему аутентификации

Необходимо реализовать JWT-аутентификацию для API endpoints.

Требования:
- Login endpoint
- JWT токены с истечением срока
- Middleware для проверки токенов
```

### Поля задачи

| Поле | Описание | Возможные значения |
|------|----------|-------------------|
| `title` | Название задачи | Текст |
| `status` | Текущий статус | `New`, `In Progress`, `Done`, `Failed` |
| `priority` | Приоритет | `Low`, `Medium`, `High` |

### Примеры задач

**Пример 1: Простая задача**
```markdown
---
title: Fix typo in README
status: New
priority: Low
---

Исправить опечатку в секции "Installation" файла README.md
```

**Пример 2: Сложная задача**
```markdown
---
title: Refactor user service
status: New
priority: High
---

Рефакторинг User Service:

1. Разделить на отдельные модули
2. Добавить валидацию входных данных
3. Обновить тесты
4. Добавить документацию

Ожидаемый результат:
- Улучшенная читаемость кода
- Покрытие тестами > 80%
```

## Запуск агента

### Одиночная задача

1. Откройте список задач для вашего проекта
2. Найдите нужную задачу со статусом "New"
3. Нажмите кнопку "Run" рядом с задачей

### Несколько задач

1. Поставьте чекбоксы напротив задач, которые хотите запустить
2. Нажмите кнопку "Run Selected" вверху списка

### Что происходит при запуске

1. **Создание Worktree**: Система создает изолированный Git Worktree для каждой задачи
2. **Запуск агента**: Запускается процесс `opencode` в папке Worktree
3. **Обновление статуса**: Задача меняет статус на "Running"
4. **Транслирование логов**: Вывод агента отображается в реальном времени

## Мониторинг выполнения

### Статусы задач

- **New** - Новая задача, готова к запуску
- **Running** - Агент выполняет задачу
- **Done** - Задача успешно завершена
- **Failed** - Задача завершилась с ошибкой

### Просмотр логов

1. Нажмите на задачу, чтобы открыть детали
2. В разделе "Logs" вы увидите вывод терминала агента в реальном времени
3. Логи обновляются автоматически во время выполнения

### Индикаторы прогресса

- Список задач автоматически обновляется при изменении статусов
- Цветовые индикаторы показывают текущее состояние:
  - 🔵 Blue - Running
  - 🟢 Green - Done
  - 🔴 Red - Failed
  - ⚪ Gray - New

## Проверка результатов

### Доступ к Worktree

После завершения задачи (статус "Done"):

1. Найдите папку Worktree:
   ```
   /путь/к/проектu/worktrees/feat-task-name/
   ```
2. Откройте её в вашей IDE или терминале

### Проверка изменений

```bash
cd /путь/к/проектu/worktrees/feat-task-name/
git diff main  # Посмотреть изменения
git log        # Посмотреть историю коммитов
```

### Запуск тестов

```bash
npm test  # или ваша команда для запуска тестов
```

### Принятие или отклонение

**Если результат устраивает:**

```bash
# Слить в основную ветку
git checkout main
git merge worktrees/feat-task-name

# Или создать Pull Request
# (в зависимости от вашего workflow)
```

**Если нужно доработать:**

1. Внесите изменения вручную в Worktree
2. Или перезапустите агента с уточненной задачей
3. Повторите проверку

### Удаление Worktree

После успешного слияния:

```bash
git worktree remove worktrees/feat-task-name
```

## Советы и рекомендации

### Создание задач

- **Будьте конкретны**: Четко опишите, что нужно сделать
- **Разбивайте большие задачи**: Лучше несколько маленьких, чем одна огромная
- **Указывайте приоритеты**: Это поможет выбрать порядок выполнения

### Запуск задач

- **Не перегружайте систему**: Запускайте 2-3 задачи параллельно
- **Мониторьте ресурсы**: Следите за использованием CPU и памяти
- **Проверяйте логи**: Смотрите логи, если задача застряла

### Проверка результатов

- **Всегда тестируйте**: Не мерьте изменения без проверки
- **Сравните с оригиналом**: Используйте `git diff` для сравнения
- **Сохраняйте Worktree**: Не удаляйте Worktree, пока не уверены в результате

## Устранение проблем

### Задача не запускается

1. Проверьте, что путь к проекту указан верно
2. Убедитесь, что папка `.memory_bank/tasks/` существует
3. Проверьте формат MD-файла задачи

### Агент застрял

1. Откройте логи задачи
2. Проверьте, что агент не ждет ввода пользователя
3. При необходимости остановите задачу и перезапустите

### Статус не обновляется

1. Проверьте соединение с backend
2. Обновите страницу (F5)
3. Если проблема сохраняется, перезапустите backend

## Дополнительные ресурсы

- [Архитектура системы](architecture.md)
- [API Reference](api.md)
- [Руководство для разработчиков](development.md)
